<p id="notice"><%= notice %></p>
<% if flash[:suzuri_response] %>
  以下のアクリルキーホルダーが作成されました。<br>
  <%= link_to image_tag(flash[:suzuri_response][:products][0][:sampleImageUrl]),flash[:suzuri_response][:products][0][:sampleUrl] %><br>
  <%= link_to 'SUZURIの商品詳細ページへ', flash[:suzuri_response][:products][0][:sampleUrl] %><br>
<% end %>

<h1>名前入りSUZURIグッズジェネレーター</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @keychains.each do |keychain| %>
      <tr>
        <td><%= link_to 'Show', keychain %></td>
        <td><%= link_to 'Edit', edit_keychain_path(keychain) %></td>
        <td><%= link_to 'Destroy', keychain, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新しく名前入りのアクキーを作る', new_keychain_path %>
